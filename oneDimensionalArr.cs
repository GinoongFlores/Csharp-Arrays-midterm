namespace Arrays
{
    public class oneDimensionalArr
    {
        public static void Main(string[] args)
        {
            String[] names = { "Jason", "Philip", "Jaycon", "Jeeza", "Jovel", "Lorence" };
            foreach (String name in names)
            {
                Console.WriteLine(name);
            }
        }
    }
}